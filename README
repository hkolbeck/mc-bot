To build, first install go-ircbot from http://github.com/ckolbeck/go-ircbot, then run `make mcserver && make`.

Several values, such as the location to write backups, and the name of the admin, are currently hardcoded.  They may be found at the top of mcbot.go.